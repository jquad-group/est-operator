---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: estorders.certmanager.jquad.rocks
spec:
  group: certmanager.jquad.rocks
  names:
    kind: EstOrder
    listKind: EstOrderList
    plural: estorders
    singular: estorder
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: EstOrder is the Schema for the estorders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EstOrderSpec defines the desired state of EstOrder
            properties:
              issuerRef:
                properties:
                  group:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                required:
                - group
                - kind
                - name
                type: object
              renewal:
                type: boolean
              request:
                description: The signed PKCS#10 request in PEM encoding, and then
                  base64 encoded. This value is automatically generated by cert-manager
                  and is copied from the CertificateRequest
                type: string
            required:
            - issuerRef
            - request
            type: object
          status:
            description: EstOrderStatus defines the observed state of EstOrder
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
